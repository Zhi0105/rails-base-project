<div class="bg-white pb-4 px-8 rounded-md w-full">
    <div class="overflow-x-auto mt-6">
        <h1 class="text-3xl font-bold text-violet-600 pb-1">Trader Transaction Logs</h1>
        <table class="table-auto border-collapse w-full">
            <thead>
            <tr class="rounded-lg text-white text-sm font-medium text-left bg-gradient-to-bl from-gray-900 via-purple-900 to-violet-600" style="font-size: 0.9674rem">
  
                <th class="px-4 py-2">Log message</th>
                <th class="px-4 py-2">Date</th>
            </tr>
            </thead>
            <tbody class="text-sm font-normal text-gray-700">
            <% @transactions.each do |t| %>
                <tr class="hover:bg-gray-100  border-gray-200">
                    <td class="px-4 py-4">
                        <%= "#{get_trader_full_name(t.trader_id)} has 
                            #{ "bought" if t.transaction_type == "BUY"
                            }
                            #{ "sold" if t.transaction_type == "SELL"
                            }
                            #{ t.unit} unit of
                            #{get_market_name(t.market_symbol)} with total amount of
                            #{number_to_currency(t.revenue, precision: 2, unit: "₱ ")}
                        "%>
                    </td>
                    <td class="px-4 py-4"><%= t.created_at.strftime('%a %d %b %Y') %></td>
                </tr>
            <% end %>
            </tbody>
        </table>
    </div>
</div>
